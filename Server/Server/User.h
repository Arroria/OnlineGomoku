#pragma once
#include <functional>
class User
{/*
public:
	User();
	~User();
	

	inline void Run()		{ m_connector.Run(); }
	inline bool Joinable()	{ return m_connector.Joinable(); }
	inline void Join()		{ m_connector.Join(); }
	inline void Detach()	{ m_connector.Detach(); }


	inline const SOCKET&		Socket() const	{ return m_connector.Socket(); }
	inline const sockaddr_in&	Address() const	{ return m_connector.Address(); }

	inline void MsgProcesser(AsyncConnector::Returner_t& processer) { m_connector.Returner(processer); }


	inline bool operator==(const User& _other) const { return m_connector == _other.m_connector; }
	inline bool operator>(const User& _other) const { return m_connector > _other.m_connector; }
	inline bool operator<(const User& _other) const { return m_connector < _other.m_connector; }
	
private:
	AsyncConnector m_connector;*/
};

